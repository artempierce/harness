[project]
name = "harness"
version = "0.1.0"
description = "An agent harness, built layer by layer, to learn how one works."
requires-python = ">=3.11"
dependencies = ["anthropic>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
